package labs.lab13studentinterface;

interface StudentInterface {
    //TODO 4: add result() and fee() as abstract methods
    // methods in Interfaces are public and abstract by default(you don't have to explicitly use the “abstract” keyword
    abstract void result();
    abstract void fee(int fee);
}